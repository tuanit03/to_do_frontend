{
  "name": "to_do_app",
  "devDependencies": {
    "@aws-amplify/backend": "^1.6.0",
    "@aws-amplify/backend-cli": "^1.4.0",
    "aws-cdk": "^2.165.0",
    "aws-cdk-lib": "^2.165.0",
    "constructs": "^10.4.2",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "aws-amplify": "^6.8.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r !(dist) dist/"
  }
}
